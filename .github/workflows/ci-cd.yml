name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Required to push the commit back to the repo

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract Metadata (Tags, Labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: rodas01/seb-exam-portal
          tags: |
            type=sha,prefix= # Outputs short 7-char SHA (e.g., 9fb8b3e)
            latest

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=registry,ref=rodas01/seb-exam-portal:buildcache
          cache-to: type=registry,ref=rodas01/seb-exam-portal:buildcache,mode=max

      # --- GitOps Step: Update Deployment Manifest ---
      - name: Update Image Tag in Kubernetes Manifest
        env:
          SHA_TAG: ${{ steps.meta.outputs.version }}
        run: |
          # Use SHA_TAG (short SHA from metadata-action) to match Docker Hub
          sed -i "s|image: rodas01/seb-exam-portal:.*|image: rodas01/seb-exam-portal:${SHA_TAG}|g" k8s/rollout.yaml

      - name: Commit and Push Updated Manifest
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "ci: update rollout image tag to ${{ steps.meta.outputs.version }}"
          file_pattern: "k8s/rollout.yaml"
